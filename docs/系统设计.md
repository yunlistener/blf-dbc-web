# BLF/DBC CAN 总线分析平台 — 系统设计

> 版本:v1(2026-08-23)
> 依据:《需求文档》v84+(含新增"播放模式 + 动态解析推送"需求 §3)
> 原则:**抛开现有架构与代码**,按需求重新设计;核心目标 = 离线回放与实时接收共用同一套前后端管线

---

## 1. 设计目标与约束

| 目标 | 说明 |
|---|---|
| ① 对齐 CANoe 流程 | 配置网络 → 添加信号 → **播放** → 曲线动态显示 |
| ② 前后端一致接口 | 离线 BLF 回放与后期实时 CAN 接收,共用同一"帧批次推送 + 增量渲染"管线 |
| ③ 大数据量流畅 | 百万点级渲染(uPlot 保形降采样)、GB 级 BLF 流式处理 |
| ④ 易部署 | ARM64 免编译,单进程 systemd 常驻,局域网访问 |
| ⑤ 可扩展 | 数据源可插拔(离线/实时/未来更多),多用户(1-5 人) |

**约束**:数据不出局域网;内存峰值 ≤1GB;DBC 兼容 GBK/UTF-8;前端 Edge 缓存需版本号击穿。

---

## 2. 总体架构

```
┌──────────────────────────────────────────────────────────┐
│ 前端(浏览器)                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ 配置/文件 │ │ 信号树   │ │ 示波器区 │ │ Trace/统计   │ │
│  │ UI       │ │ + 已选列 │ │ (多窗口) │ │ /导出        │ │
│  └──────────┘ └──────────┘ └────┬─────┘ └──────────────┘ │
│  ┌──────────────────────────────▼──────────────────────┐ │
│  │ 播放控制台(状态机):▶ 播放 / ⏸ 暂停 / ⏹ 停止 / 进度条 │ │
│  │ StreamRenderer:帧批次 → 增量渲染(uPlot append)      │ │
│  │ WS 客户端(帧流) + REST 客户端                        │ │
│  └──────────────────────────────┬──────────────────────┘ │
└─────────────────────────────────┼─────────────────────────┘
                    REST(配置/统计/导出)   WS(控制 + 帧流)
┌─────────────────────────────────▼─────────────────────────┐
│ 后端(FastAPI 单进程)                                       │
│  ┌────────────┐ ┌────────────┐ ┌────────────────────────┐ │
│  │ REST API   │ │ WS 网关    │ │ 回放引擎 PlaybackEngine│ │
│  │ 文件/DBC   │ │ /ws/replay │ │ 速率控制/播放状态/批量 │ │
│  │ 解码/统计  │ │ /ws/live   │ │ 推送/订阅过滤/seek     │ │
│  │ /导出/配置 │ │ (广播帧流) │ └───────────┬────────────┘ │
│  └────────────┘ └────────────┘             │ FrameSource   │
│  ┌─────────────────────────────────────────▼────────────┐ │
│  │ 服务层:FileService / DbcService / DecodeService      │ │
│  │        StatsService / ExportService                  │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────┬─────────────────────────┘
                                  │ FrameSource 统一接口
┌─────────────────────────────────▼─────────────────────────┐
│ 数据源层(可插拔)                                          │
│  ┌──────────────────┐  ┌──────────────────────────────┐   │
│  │ BlfReplaySource  │  │ CanLiveSource(后期)          │   │
│  │ 离线 BLF 回放    │  │ socketcan 实时接收            │   │
│  │ (帧索引缓存加速) │  │ (同 FrameSource 接口)        │   │
│  └──────────────────┘  └──────────────────────────────┘   │
│  帧索引缓存(内存 LRU + 磁盘索引可选)                        │
└──────────────────────────────────────────────────────────┘
```

**核心思想**:
1. **数据源可插拔**:`FrameSource` 统一接口,离线回放和实时接收都是它的实现 → 后端引擎与前端渲染完全不知道数据来自文件还是总线
2. **一条渲染管线**:前端 `StreamRenderer` 只消费"帧批次",播放模式和实时模式零区别。渲染策略:播放时 **x 轴固定全量范围(时间轴不动,曲线在固定时间轴上往后画)**,前端以 **rAF 毫秒级循环**取累积数据增量 setData(非粗粒度节流),保证曲线平滑无跳跃。同窗多信号渲染:各信号时间戳浮点不重合时,采用 **时间桶对齐**(自适应桶 = 信号中位周期一半,上限 5ms;时间戳量化到桶网格)——同窗信号同桶,x 并集不翻倍,per 数组 100% 非 null、零填充,连续线且**采样率零增加**;读数/tooltip 用 posToIdx;值表信号用采样点 mask 标记。uPlot 1.6 不支持嵌套 [t,v] 格式(实测全空白),必须列格式+桶对齐;官方确认:共享 x 列格式是有意设计(性能换对齐),非 bug;scatter/独立 x 支持(issue #107)多年 on hold;官方钦定做法 uPlot.join()(对齐+null 填充),本方案桶量化不膨胀数据,优于 join
3. **控制与数据分离**:REST 管配置/统计/导出(同步),WebSocket 管帧流(异步推送)

---

## 3. 技术选型(后台选型为重点)

### 3.1 后端框架/语言

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Python + FastAPI** | async 原生 WebSocket;类型校验+Swagger;python-can/cantools 生态唯一选择 | 纯 Python 解析大文件慢(缓存/索引/C 扩展缓解) | ✅ **首选** |
| Python + aiohttp | WebSocket 好 | 无类型校验,生态弱于 FastAPI | 备选 |
| Node.js + Express/WS | 实时推送生态好 | CAN/DBC 解析库基本要自研 | ❌ |
| Go + gorilla/ws | 性能好,单二进制 | DBC/BLF 生态空白,开发成本高 | ❌ |

**结论:Python 3.11+ / FastAPI / Uvicorn(单 worker)**。理由:
- 生态决定论:python-can(BLF)+ cantools(DBC)没有替代
- FastAPI 原生支持 WebSocket 双向通信(播放控制 + 帧流推送),异步模型适合流式回放
- 性能短板通过"帧索引缓存 + 批量解码 + 可选 Cython 热点"解决,见 §7

### 3.2 前端

| 方案 | 结论 |
|---|---|
| **原生 JS(ES Modules)+ uPlot** | ✅ 首选:uPlot 百万点级渲染无可替代;播放控制用轻量状态机;模块化拆分(不引入构建工具,静态文件直出) |
**图表库实测对比(2026-08,Edge headless,真实规模 4611点/信号,6信号典型 + 64信号30万点上限):**

| 库 | 体积 | init6 | zoom6 | init64 | zoom64 | 独立x |
|---|---|---|---|---|---|---|
| **Lightweight Charts** | 163KB | 9.7ms | **0.1ms** | **30.8ms** | **0.1ms** | ✅每series独立 |
| Chart.js 4 | 205KB | 3.6ms | 1.8ms | 52.8ms | 10.4ms | ✅[x,y]对 |
| ECharts 5 | 1MB | 27.1ms | 6.7ms | 89.7ms | 31.1ms | ✅data对 |
| Plotly 2 | 4.36MB | 39.1ms | 5.3ms | 54.5ms | 15.6ms | ✅x/y独立 |
| uPlot 1.6(现状) | 50KB | ~5ms | ~1ms | ~30ms | ~5ms | ❌列格式强制对齐 |

**结论:Lightweight Charts 最优**(TradingView 出品,活跃维护):①缩放/平移只重绘可见区 → zoom 0.1ms 近乎免费,30 万点 init 30.8ms 全库最快;②每 series 独立时间戳 → 方案 A 直接可用,桶对齐可删;③自带 timeScale 时间轴交互(金融级缩放/平移);④163KB。适配点:time 需严格递增整数毫秒(0.01s=10ms ✓)、值表阶梯线需近似(无 stepped 模式,用 line)、锚点竖线/光标同步需 overlay 自绘(有 createPriceLine 水平线 + crosshair 事件)。

**⚠️ LWC 播放硬伤(实测确认,2026-08)→ 已排除:**
LWC 4.2/5.0.8 是"实时行情"语义:任何 series 数据不满全量时间范围时,setVisibleRange/setVisibleLogicalRange/fitContent 全部失效(可见范围锁在数据末尾,实测 4611 点骨架 + 922 点真实 series → 锁 4511~4610)。播放时曲线在视野外、时间轴表现异常。骨架/延迟设置/minBarSpacing/undefined 占位等方案全部验证无效。ECharts/Chart.js/Plotly 的 axis range 是声明式,数据部分时完全自由(均已实测验证播放可行)。

**外部选型指南参考(2026-06 CSDN《图表库选型终极指南》,8 库 2026-06 实测,gzip 体积):**

| 库 | 版本 | 授权 | gzip 体积 | 周下载 | 维护方 |
|---|---|---|---|---|---|
| Chart.js | 4.5.1 | MIT | 68KB | 1316万 | 社区 |
| Highcharts | 13.0.0 | **商业**(个人免费) | 99KB | 242万 | Highsoft |
| Victory | 37.3.6 | MIT | 112KB | 33万 | Formidable(React) |
| Recharts | 3.9.1 | MIT | 150KB | 5288万 | 社区(React) |
| AntV G2 | 5.4.8 | MIT | 321KB | 35万 | 蚂蚁集团 |
| Apache ECharts | **6.1.0** | Apache-2.0 | 360KB(按需150-200KB) | 362万 | Apache 基金会 |
| Plotly.js | **3.6.0** | MIT | 363KB | 81万 | Plotly |
| D3.js | 7.9.0 | ISC | 90KB(按需20-40KB) | 1443万 | Bostock+社区 |

**对本项目(内网 CAN 示波器)相关要点:**
1. **ECharts 6.x 大数据优化**:`large: true` + `progressive: 5000` 渐进渲染 —— 我们实测 64 信号 30 万点 init 89.7ms 是默认模式,开启 large 后预计显著下降,30 万点场景无顾虑
2. **ECharts 体积**:gzip 360KB 全量、按需 150-200KB(实测 1MB 是原始未压缩);内网部署无感知
3. **Plotly 3.6 定位**:统计/科学计算专长,指南明确"常规业务图表用 ECharts 更顺手"
4. **AntV G2**(321KB):蚂蚁系、原生 JS 可用,但指南评价"文档生态偏、团队上手成本高、主场阿里系";对本项目无额外优势,不入围
5. **排除确认**:Highcharts(商业授权)、Recharts/Victory(React 栈)、D3(造轮子成本高)
6. **a11y 不适用**:内网工程工具无合规要求(ECharts a11y 需自定义的缺点不影响本项目)
7. **指南"别选它"映射**:ECharts"体积极致敏感/a11y"→ 内网+无合规不构成障碍;Plotly"体积敏感/常规图表"→ 与其统计科学强项不匹配;Chart.js"复杂图表/K线/地图"→ 本项目多窗口同步/锚点/阶梯线超出其轻量定位

**✅ 已定案(2026-08-23 用户拍板):Chart.js 4.5.1** —— 三库播放/阶梯线均实测可行,Chart.js 性能/体积全面领先(30万点 init 33.5ms、缩放 13.1ms、208KB);缩放/锚点用自绘 plugin(overlay afterDraw),不引第三方插件。切换完成 v99,备份 tag backup-lwc-v98 / backup-uplot-v93。

## 大文件处理改进方案(2026-08,批次 1 已实施 ✅,批次 2-4 待做)

### 批次 1 实施结果(2026-08-24,dev)
- 两级索引(通道→报文→有序帧)+ 紧凑存储(ts float32 相对时间/off int32/dlc/flags uint8 + data 拼接池)
- 一次全扫合并产出 stats+索引+通道;pickle 落盘 data/cache/{name}.idx(文件签名失效,CACHE_VERSION=2)
- `GET /api/blf/{name}/meta`:文件头零扫描(帧数/时长/时间,31ms 含部分扫描)+ 通道(缓存/部分扫描 2 万帧)
- decode 时间窗语义修正:start/end=相对秒 → 内部转绝对(索引 ts 绝对;此前相对/绝对不匹配,时间窗恒空)
- 实测(686 万帧 101MB):meta 31ms;stats 首次全扫 15.7s → 二次/重启 7ms;decode 通道+时间窗 5ms/1000 点;磁盘 222.8MB(较 pickle-list ~2GB 缩 9 倍)
- 播放复用索引正常(40 消息/746 点)
- 缓存失效:上传/删除文件 → invalidate(内存+磁盘)

### 批次 2 实施结果(2026-08-24,dev)
- `build_async()` 后台线程构建:上传 BLF / stats 无缓存 / meta 无缓存 → 自动后台构建(progress 可见),请求立即返回
- stats 无缓存返回 **202 {"status":"building"}**(2ms),前端 loadStats 轮询 1s 重试,构建完成秒回(2ms)
- 上传接口:写入后立即返回 + 后台构建(前端界面立即关闭可用)
- 实测:清缓存 → 202/2.4ms → 进度 0.1+ → 构建完成 200/2.3ms

### 批次 3 实施结果(2026-08-24,dev)
- 选信号仅订阅:addSignal 不再调 decode(曲线空),数据在播放时填充
- 播放边解码边画(现有 pump 增量,索引源)→ 曲线随播放出现;时间轴固定全量
- 停止/结束保留播放数据(曲线停在播放位置,不再恢复全量静态)
- 缩放懒加载:停止后缩放窗口超出已解码范围 → 按需 decode(start/end 相对秒)→ winData 替换显示;窗口在已解码范围内直接用播放数据
- stats 格式兼容修复:批次1 by_id dict/channels 简化/first_ts 影响前端 hasData/通道列表/ID 统计 → 前端兼容新旧格式
- 实测(headless):add=true sub-data=0;播放 px=10232 时间轴固定;停止 data=300 保留

### 批次 4 实施结果(2026-08-24,dev)—— 四批次全部完成 ✅
- BlfReplaySource 直接适配紧凑两级索引(免 get_frames_index 展开,686 万帧省 ~200MB 临时内存);seek 用 numpy searchsorted 二分
- 实测:播放 40 消息/751 点;seek(20s) batch t1=20.0 精确
- 播放进度条拖拽(seek)已有;操作流程表(用户 9 步)全对齐
- 发布:dev 合并 release

### 修复(2026-08-24 用户反馈)
- **播放空窗误判结束**:advance_to 中 next_batch 空即返回 None(播完)→ 全日志无帧时段(信号丢失期)被误判结束,进度条卡住。修复:仅当 play_time >= 源末帧时间(time_range[1])才结束;空窗返回空批次跳过继续。实测:0xa8/0x122 通道0 95.8s 停发,修复后 t1 持续推进 282.8s 不结束
- **信号丢失的可视化**:订阅信号在某时段停发 → 曲线停在最后位置,进度条继续走(正确行为);若同报文在其他通道在发(实测 0xa8 通道1/2 有 11000 点),可切通道查看
- **保存配置一次生效**:saveConfig 中 blfChanged 时清空通道映射(channels:{})→ 第一次保存(选BLF+配DBC)丢映射,要点两次才刷新树。修复:始终用表单通道映射,新 BLF 不存在的通道由 loadFiles 自然忽略

## 四批次总览(2026-08-24 完成)
| 批次 | 内容 | 实测 |
|---|---|---|
| 1 | 两级索引+紧凑存储+磁盘缓存+meta 零扫描+decode 时间窗 | meta 31ms;stats 15.7s→7ms;decode 5ms;222.8MB |
| 2 | 后台异步构建(上传/打开不阻塞) | 202/2.4ms → 构建完 200/2ms |
| 3 | 选信号仅订阅、播放边解码边画、缩放懒加载 | add 0 数据;播放渲染;停止保留 |
| 4 | 播放源直接适配紧凑结构、发布 release | 播放+seek 正常 |

## 大文件处理改进方案(2026-08,全部实施完成 ✅)
- 批次 2:后台异步构建(上传/打开无缓存 → 线程构建,前端不阻塞;start_build/finish_build 骨架已就位)
- 批次 3:前端选信号仅订阅,播放边解码边画,缩放懒加载
- 批次 4:播放源直接适配紧凑结构(去掉 get_frames_index 展开)、文档收尾、发布 release


### 目标(用户操作流程对齐)
1. 打开页面:不触发全扫 —— 读磁盘缓存,秒开
2. 上传文件:立即返回 —— 后台异步构建索引,前端不阻塞
3. 选择 BusLog:只要基本信息 + 通道列表(配置 DBC 用)—— 缓存秒回
4. 保存配置 DBC 变化 → 刷新左侧信号树(已实现 ✓)
5. 第 4 步选信号:仅订阅不画曲线(现状会全段解码)—— 播放时边解码边画

### 现状(代码核对)
- 全扫 ×2:页面加载 stats① + 首次解码索引②(播放复用索引,无第三次全扫)
- decode 全段解码:选信号即整段日志解码 + 降采样
- 索引仅内存缓存(后端重启/树莓派重启全丢)

### 方案(阶段 1:一次全扫 + 磁盘缓存 + 后台异步)
1. **stats + 索引 + 通道列表合并为一次全扫**,结果 pickle 落盘 `data/cache/{blf}.idx`(文件 size+mtime 签名失效)
2. **后台异步构建**:上传完成 / 打开页面发现无缓存 → 后台线程建索引(复用 progress 进度跟踪),前端立即返回,页面可先用(遮罩进度条已有)
3. **meta 接口**(`GET /api/blf/{name}/meta`):基本信息(帧数/时长/通道列表)→ 缓存秒回;无缓存返回"构建中"状态,前端提示
4. 打开页面:有缓存 → 直接读(秒开,不触发任何扫描)
5. 保存配置:BLF 变 → 触发异步构建;DBC 变 → 刷新信号树(已实现)

### 阶段 2:按需解码(第 4 步订阅,第 5 步边播边解码)
- 选信号:仅订阅(不调 decode,不画曲线)
- 播放:后端按播放进度增量解码订阅信号(现有 pump 已是增量,源读索引)→ 曲线随播放出现
- 播放停止/结束:已解码数据保留,静态视图直接复用
- 缩放/拖拽看未解码区域 → 按需补解码该窗口(懒加载)

### 关键约束(2026-08-24 实测修正)
- **BLF 文件头零扫描可拿**(python-can 4.6 BLFReader.__init__ 解析 52 字节头):object_count(总对象数≈帧数,误差<0.1%)、start/stop_timestamp(时长,精确)、文件大小 → meta 接口 <1ms 秒回
- **通道列表必须扫描**(通道在帧数据里)→ 部分扫描前 2 万帧(毫秒级,多通道同步记录近似全覆盖)+ 后台全扫修正
- 完整 stats(按 ID 计数/错误帧/通道分布/bus load)仍需全扫一次 → 后台异步 + 磁盘缓存
- 内存预算:索引常驻(MAX_FRAMES 限制保留);磁盘缓存解决重启重建
- 树莓派:首次全扫预计 1-3 分钟(带进度条),之后全秒回

**Chart.js 渲染层要点(v99-v109 实测踩坑固化):**
- 销毁方法 **destroy()**(非 remove());tooltip 回调引用函数必须先验证存在(fmtNum 教训:图表每次 update 触发 tooltip 回调,未定义函数 → 渲染中断 → 播放失效)
- scales.x.top/bottom 是轴位置非绘图区 → overlay 竖线用 chart.chartArea
- 播放固定时间轴:scales.x.min/max 声明式(数据部分时保持,实测 [0,46.1])
- 值表信号:cantools NamedSignalValue 必须转数值(to_plain,静态/播放共用)—— 播放路径手动 json.dumps 会直接崩
- 缩放:x 范围钳制 [0,dur];普通滚轮不拦截(页面滚动),Ctrl+滚轮缩放
- 事件循环:前端渲染阻塞 → WS 不读 → TCP 缓冲满 → 后端 pump send 挂起 → 播放停(诊断条定位)

**最新版三库实测(2026-08-23,Edge headless 1600px,4611点/信号,ECharts 6.1.0 / Chart.js 4.5.1 / Plotly 3.6.0):**

| 指标 | ECharts 6.1(1.12MB) | Chart.js 4.5(208KB) | Plotly 3.6(4.84MB) |
|---|---|---|---|
| 6 信号 init | 15.0ms | **1.3ms** | 24.9ms |
| 播放 922 点 xAxis | [0,46.1] ✅ | [0,46.1] ✅ | [0,46.1] ✅ |
| 播放 1500 点 xAxis | [0,46.1] ✅ | [0,46.1] ✅ | [0,46.1] ✅ |
| 播放数据更新 | — | **1.6ms** | 5.1ms |
| 值表阶梯线 | step ✅ | stepped ✅ | shape:'hv' ✅ |
| 64 信号 30万点 init | 86.6ms(large 模式) | **33.5ms** | 64.1ms |
| 0.5s 缩放(30万点) | 36.1ms | **13.1ms** | 15.5ms |

**实测观察:**
1. **播放固定时间轴 + 阶梯线:三库全部验证通过**(此前的关键需求已无差异)
2. **ECharts 6.1 `large:true` 收益有限**(30万点 86.6ms vs 5.5 默认 89.7ms,主要优化渲染内存非速度);**0.5s 缩放 36.1ms 是三库最慢**(播放流式每帧 setOption 有压力)
3. **Chart.js 4.5 性能/体积全面领先**(init 1.3ms、30万点 33.5ms、缩放 13.1ms、208KB);代价:缩放/锚点依赖第三方插件(chartjs-plugin-zoom / @chartjs/annotation)
4. **Plotly 3.6 体积 4.84MB 最大**(3.x 未拆小),性能稳定中等,播放更新 5.1ms
5. **结论修正**:纯性能 Chart.js 最优;但插件依赖 vs 内置能力(ECharts/Plotly)的工程权衡不变 —— 最终三选一仍待用户拍板

| 指标(4611点/信号) | ECharts 5.5(1MB) | Plotly 2.35(4.36MB) |
|---|---|---|
| 6 信号 init | 27.1ms | 39.1ms |
| 6 信号 0.5s 缩放 | 6.7ms | 5.3ms |
| 64 信号 init(30万点) | 89.7ms | 54.5ms |
| 64 信号 0.5s 缩放 | 31.1ms | 15.6ms |

**结论:两库均可行,且都支持每 series 独立 x(方案 A 直接可用,桶对齐可删)。** 在**限制最小缩放窗口**(如 0.5s,100Hz → 50 点/信号)方案下,同屏点数可控,两库缩放 5-30ms 均流畅;即使全量 30 万点一次性渲染也在 90ms 内。取舍:ECharts 体积小 4 倍、中文生态、series 独立 data;Plotly 大场景更快、学术标准但体积大。切换成本=前端渲染层重写(plots/光标同步/tooltip/锚点/播放 StreamRenderer 适配)。 Plotly.js 每 trace 独立 x 数组(天然支持不对齐数据,方案 A 直接可行),但性能不可接受:①体积 4.36MB vs uPlot 50KB(87 倍);②30 万点(64 信号×4611)SVG 模式卡顿明显,Scattergl(WebGL)能扛但 hover/读数/框选交互受限;③播放流式 extendData 全量重算布局,多 trace 更新卡。桶对齐后单信号 4611 点 = uPlot 标准列格式,零性能妥协;未来百万点 uPlot 仍最优。
| Vue 3 + Vite | 备选:若 UI 复杂度(多窗口拖拽/配置表单)继续上升再引入 |

前端模块划分:`api.js`(REST+WS 客户端)、`renderer.js`(StreamRenderer)、`playback.js`(播放状态机)、`plots.js`(多示波器管理)、`stats.js`(统计 tab)、`app.js`(装配)。

### 3.3 传输协议

| 场景 | 协议 | 理由 |
|---|---|---|
| 配置/统计/导出/文件 | REST(JSON) | 同步、幂等 |
| **帧流推送** | **WebSocket** | 需要双向:前端发 play/pause/seek 控制 + 后端持续推帧批次 |
| 心跳/断线重连 | WS ping/pong + 自动重连 | 回放中断可续 |

**帧批次协议**(前后端一致接口):
```jsonc
{
  "type": "batch",
  "seq": 42,                    // 批次序号(检测丢包)
  "t0": 1718000000.000,         // 回放起点(绝对秒,用于时钟对齐)
  "rate": 1.0,                  // 当前回放速率
  "frames": [
    { "ts": 1.234, "id": "0x21", "data": "AA BB CC", "channel": 0, "is_fd": true }
  ],
  "signals": {                  // 已订阅信号的解码值(增量)
    "ACU_3_CRC1": 190,
    "ACU_3_Resd1": 0
  }
}
```
前端只消费 `frames`/`signals` 追加到 uPlot,`seq` 用于断点续推;实时模式(后期)推送同样的 `batch`,前端零改动。

### 3.4 缓存与存储

| 层 | 方案 | 理由 |
|---|---|---|
| 帧索引 | 内存 LRU(4 文件/200 万帧)+ 磁盘索引可选(mmap) | 回放逐帧读取要随机定位,索引是回放性能地基 |
| 解码缓存 | 信号级 LRU(带 mtime/size 校验) | 统计/静态分析复用 |
| 配置 | JSON 文件(`data/config.json`) | 简单;无 DB 必要 |
| 元数据/分析结果 | 内存 + 可选 SQLite(文件列表/分析快照) | 1-5 用户,DB 非必需,先不引入 |

### 3.5 部署

- systemd + Uvicorn 单进程(回放引擎/数据源在进程内,状态简单)
- 静态文件由 Uvicorn 托管;版本号 `?v=N` 击穿 Edge 缓存
- 后期实时接收:同一进程内 `CanLiveSource` 线程/异步读 socketcan

---

## 4. 核心抽象:FrameSource

```python
# 统一帧源接口(离线/实时)
class FrameSource(ABC):
    def open(self) -> None: ...            # 打开数据源
    def next_batch(self, n: int) -> list[Frame]:   # 取下一批帧
    def seek(self, t: float) -> None: ...  # 定位到时间点(回放进度条)
    @property
    def time_range(self) -> tuple[float, float]: ...  # 数据时间范围
    def close(self) -> None: ...
    @property
    def is_live(self) -> bool: ...         # 实时源(无 seek,时间跟随)
```

- `BlfReplaySource`:帧索引缓存加速的离线回放;`seek` 用索引二分定位
- `CanLiveSource`(后期):socketcan 读取,`is_live=True`,无 seek

**回放引擎只依赖接口**:速率控制(时间缩放)、订阅过滤(只解码已选信号)、批量推送都建立在 `next_batch` 之上;实时源接入时引擎/前端零改动。

---

## 5. 后端模块设计

### 5.1 模块清单

| 模块 | 职责 |
|---|---|
| `app/main.py` | FastAPI 装配、静态托管、WS 路由注册 |
| `app/api/` | REST:files/dbc/blf/export/config |
| `app/ws/` | WS 网关:连接管理、会话(回放状态)、帧流广播 |
| `app/core/playback.py` | PlaybackEngine:速率/播放状态机/seek/订阅 |
| `app/sources/` | FrameSource 实现:blf_replay.py、can_live.py(后期) |
| `app/services/` | file/dbc(decode 编码检测)/decode/stats(信号/周期/负载/超范围)/export |
| `app/core/cache.py` | 帧索引缓存(LRU + 磁盘可选) |

### 5.2 回放会话(WS)

- 每个 WS 连接 = 一个回放会话:`{source, engine, subscriptions}`
- 控制消息:`{"cmd":"play","rate":1.0}` / `{"cmd":"pause"}` / `{"cmd":"stop"}` / `{"cmd":"seek","t":12.3}` / `{"cmd":"subscribe","signals":[...]}`
- 帧批次按回放速率定时推送(如 50ms 一批,批内帧数 = 速率×时间窗);订阅信号实时解码进 `signals` 字段
- 断线重连:`seq` 续推(客户端告知最后收到的 seq,从该处续)

### 5.3 分析服务(静态,同步)

- signal-stats(min/max/mean/std/超范围/值表分布)
- cycle-stats(期望/实测周期/峰峰抖动/丢帧/峰值时间)
- bus-load(CAN/CAN FD 位时近似)
- 全部走帧索引缓存,0.02s 级响应

---

## 6. 前端模块设计

| 模块 | 职责 |
|---|---|
| `playback.js` | 播放状态机(play/pause/stop/seek/rate)、进度条 UI |
| `renderer.js` | **StreamRenderer**:接收 batch → 追加 uPlot 数据 → 动态时间轴滚动;实时/回放同一入口 |
| `plots.js` | 多示波器管理(窗口增删、信号分配、y 轴聚合、时间同步、✕删除) |
| `api.js` | REST + WS 客户端(重连/seq 续推) |
| `stats.js` / `app.js` | 统计 tab / 装配 |

**StreamRenderer 关键设计**:
```js
// 统一渲染入口 —— 回放与实时都调它
renderer.push(batch) {
  // 1. 按 batch.signals 追加到对应示波器 uPlot 数据(增量 append)
  // 2. 动态 x 轴:回放模式跟随播放头;实时模式窗口滚动(如最近 30s)
  // 3. 进度条/时间显示更新
}
```

---

## 7. 性能设计

| 场景 | 方案 |
|---|---|
| GB 级 BLF 回放 | 帧索引缓存(首次全扫建索引,后续随机定位 O(log n));批读取 |
| 解码瓶颈 | 批量解码(一帧多信号一次解);热点函数可选 Cython/pyximport |
| 渲染 | uPlot min/max 保形降采样;增量 append 而非全量 setData |
| 回放速率 | 引擎按速率调度批推送,不丢帧(速率低则合并,高则抽样上限) |
| 内存 | 索引 + 解码缓存 LRU 上限;流式批处理不驻留全量帧 |

---

## 8. 接口一览

### REST(同步)
`/api/health` · `/api/files`(GET/POST/DELETE) · `/api/dbc/{dbc}/messages[/{frame_id}]` · `/api/blf/{blf}/decode|frames|signal-stats|cycle-stats|bus-load|export` · `/api/config`(GET/PUT)

### WebSocket(异步)
`/ws/replay`(离线回放:控制 + 帧流)
`/ws/live`(后期实时:帧流,同协议)

---

## 9. 开发里程碑(M4/M5 拆分)

| 迭代 | 内容 | 验收 | 状态 |
|---|---|---|---|
| **M4-1 后端帧源** | FrameSource 接口 + BlfReplaySource(索引缓存)+ PlaybackEngine(速率/seek) | curl 级:WS 推送帧批次 | ✅ v=85 |
| **M4-2 WS 网关** | /ws/replay 控制 + 推送 + seq 续传 | 断线重连续推 | ✅ v=85 |
| **M4-3 前端播放** | 播放控制台 UI + StreamRenderer 增量渲染 + 进度条 | 点播放曲线动态出现;暂停/停止/拖动可用 | ✅ v=85 |
| **M4-4 联动** | 播放模式与静态分析并存(统计/导出仍走 REST) | 全流程验证 | ✅ v=85 |
| **M5** | CanLiveSource(socketcan)+ /ws/live;前端零改动接入实时 | 树莓派接 CAN 收发器,实时曲线 | 🔮 待开工 |

> **落地说明(v=85)**:设计已实现 —— 后端 `services/frame_source.py`(FrameSource/BlfReplaySource,相对时间帧流 + 堆合并 + 二分 seek)、`services/playback.py`(PlaybackEngine,按通道 DBC 映射)、`api/ws.py`(/ws/replay,play/pause/stop/seek/rate + 批次推送);前端 `playState` + StreamRenderer(200ms 节流增量渲染)+ 播放控制栏。帧批次协议与 §7 一致。关键修复:wall_start 初始化、堆时间戳相对化。

---

## 10. 与旧架构的差异(设计取舍)

| 维度 | 旧架构 | 新设计 |
|---|---|---|
| 数据获取 | 一次性 REST decode 全量曲线 | **WebSocket 帧流 + 增量渲染**(播放/实时) |
| 数据源 | 硬编码 BLF | **FrameSource 可插拔**(离线/实时统一) |
| 前端渲染 | 一次性 setData | **StreamRenderer 增量 append** |
| 播放控制 | 无 | **播放状态机 + 进度条 + 速率** |
| 后端 | 单模块 API | 分层(api/ws/core/sources/services) |
| 静态分析 | 保留 | 保留(与播放并存) |

---

*系统设计 v1.1(2026-08-23):M4 播放模式已落地实现(v=85),见 §9 状态列与落地说明。*
